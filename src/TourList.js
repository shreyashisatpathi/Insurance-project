import React from "react";
import Itenary from "./Itenary";

const Tours = ({tours})=>{
    return (
        <div>
        {/* <h1>this is tourlist app</h1>
        <hr/>
        <div>{tours.map((tour)=>{
           console.log(tour)
           return<Itenary key ={tour.id} {...tour}/>
        }

        )
            }
        </div> */}
    
        </div>
    )
};
export default Tours;